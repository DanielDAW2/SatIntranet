<form method="post" action="{{ path('conf_cierre_reparacion_delete', {'id': conf_cierre_reparacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conf_cierre_reparacion.id) }}">
    <button class="btn">Delete</button>
</form>
