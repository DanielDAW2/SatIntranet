{% extends 'master.html.twig' %}

{% block title %}ConfCierreReparacion{% endblock %}

{% block content %}
    <h1>ConfCierreReparacion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ conf_cierre_reparacion.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('conf_cierre_reparacion_index') }}">back to list</a>

    <a href="{{ path('conf_cierre_reparacion_edit', {'id': conf_cierre_reparacion.id}) }}">edit</a>

    {{ include('conf_cierre_reparacion/_delete_form.html.twig') }}
{% endblock %}
