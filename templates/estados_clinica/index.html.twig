{% extends 'master.html.twig' %}

{% block title %}EstadosClinica index{% endblock %}

{% block content %}
    <h1>EstadosClinica index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for estados_clinica in estados_clinicas %}
            <tr>
                <td>{{ estados_clinica.id }}</td>
                <td>{{ estados_clinica.nombre }}</td>
                <td>
                    <a href="{{ path('estados_clinica_show', {'id': estados_clinica.id}) }}">show</a>
                    <a href="{{ path('estados_clinica_edit', {'id': estados_clinica.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('estados_clinica_new') }}">Create new</a>
{% endblock %}
