{% extends 'master.html.twig' %}

{% block title %}Tiquets{% endblock %}

{% block content %}
    <h1>Tiquets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tiquet.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ tiquet.numero }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ tiquet.fecha ? tiquet.fecha|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tiquets_index') }}">back to list</a>

    <a href="{{ path('tiquets_edit', {'id': tiquet.id}) }}">edit</a>

    {{ include('tiquets/_delete_form.html.twig') }}
{% endblock %}
