{% extends 'master.html.twig' %}

{% block title %}EstadosCentral{% endblock %}

{% block content %}
    <h1>EstadosCentral</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estados_central.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ estados_central.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('estados_central_index') }}">back to list</a>

    <a href="{{ path('estados_central_edit', {'id': estados_central.id}) }}">edit</a>

    {{ include('estados_central/_delete_form.html.twig') }}
{% endblock %}
