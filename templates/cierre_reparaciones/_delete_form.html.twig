<form method="post" action="{{ path('cierre_reparaciones_delete', {'id': cierre_reparacione.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cierre_reparacione.id) }}">
    <button class="btn">Delete</button>
</form>
