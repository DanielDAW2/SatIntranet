{% extends 'master.html.twig' %}

{% block title %}Marca index{% endblock %}

{% block content %}
    <h1>Marca index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Id Externo</th>
                <th>Nombre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for marca in marcas %}
            <tr>
                <td>{{ marca.id }}</td>
                <td>{{ marca.idTR }}</td>
                <td>{{ marca.nombre }}</td>
                <td>
                    <a href="{{ path('marca_show', {'id': marca.id}) }}">show</a>
                    <a href="{{ path('marca_edit', {'id': marca.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('marca_new') }}">Create new</a>
{% endblock %}
